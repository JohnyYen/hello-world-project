# Progreso del Proyecto - Frontend

## Tareas Completadas
- [x] UI-001: Landing Page pública (Hero, Características, Video) en español.
- [x] US-001: Registro de cuenta de profesor.
  - Traducido SignupForm a español.
  - Implementada validación de contraseñas (fortaleza y coincidencia).
  - Creada AuthService y SignupRequest/Response types.
  - Creada signupAction (Server Action) conectada al backend.
  - SignupForm conectado a la acción con manejo de estados y errores.
- [x] US-002: Autenticación de usuario (Login) y obtención de JWT.
  - Implementado `authService.login` en `api-client.ts`.
  - Actualizada `loginAction` para usar la API real y almacenar el token en cookies.
  - Corregidos enlaces `<a>` por `<Link>` y textos a español en login/signup.
  - Verificada la compilación exitosa del proyecto.
- [x] US-003: Cambio de contraseña segura para usuario autenticado.
  - Creada `changePasswordSchema` con validación de fortaleza de contraseña (regex) y coincidencia.
  - Implementada `changePasswordAction` (Server Action) que utiliza el paquete `@workspace/api-client-ts`.
  - Creado componente `ChangePasswordForm` con `useActionState`, `sonner` para notificaciones y validación en español.
  - Refactorizada la página `/dashboard/account` para integrar el nuevo formulario.
  - Corregidos errores de tipos y ambigüedades en el paquete auto-generado `@workspace/api-client-ts`.
  - Verificada la compilación exitosa del proyecto (build & lint).
- [x] US-004: Creación de perfil de estudiante por el profesor.
  - Actualizada `createStudentAction` en `src/lib/actions.ts` para usar `UsersApi` de `@workspace/api-client-ts`.
  - Creado componente `CreateStudentForm` usando React 19 (`useActionState`) y validación con Zod en español.
  - Integrado `CreateStudentForm` en el `Dialog` de `StudentTable` en `/dashboard/students`.
  - Verificada la compilación exitosa del proyecto (build & lint).

## Próximas Tareas
- [ ] US-005: Listado de estudiantes con paginación y filtros.

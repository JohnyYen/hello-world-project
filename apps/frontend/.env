# Client-side (browser) - visible to the browser
NEXT_PUBLIC_API_URL=http://localhost:8010
# Server-side (inside Docker) - for Server Actions and SSR
# Use http://hwp-backend:8000 when running inside Docker (Docker networking)
API_URL=http://hwp-backend:8000

JWT_SECRET=
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d
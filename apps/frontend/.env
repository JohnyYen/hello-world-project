# Client-side (browser) - visible to the browser
NEXT_PUBLIC_API_URL=http://localhost:8010
# Server-side (inside Docker) - for Server Actions and SSR
# Use localhost:8010 when running outside Docker, or http://hwp-backend:8000 when inside Docker
API_URL=http://localhost:8010

JWT_SECRET=
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d